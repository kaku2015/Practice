apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile files('src/libs/bcprov-jdk16-145.jar')
    compile files('src/libs/sun.misc.BASE64Decoder.jar')
}

// 解决控制台输入中文乱码
tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}

